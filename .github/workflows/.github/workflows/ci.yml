name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Chename: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build
        run: |
          echo "Building DevOps WebApp..."
          echo "Checking index.html..."
          test -f index.html
          echo "Build successful!"

      - name: Run tests
        run: |
          echo "Running tests..."
          test -f index.html
          echo "Tests passed!"name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build
        run: |
          echo "Building DevOps WebApp..."
          echo "Checking index.html..."
          test -f index.html
          echo "Build successful!"

      - name: Run tests
        run: |
          echo "Running tests..."
          test -f index.html
          echo "Tests passed!"ckout code
        uses: actions/checkout@v4

      - name: Build
        run: |
          echo "Building DevOps WebApp..."
          echo "Checking index.html..."
          test -f index.html
          echo "Build successful!"

      - name: Run tests
        run: |
          echo "Running tests..."
          test -f index.html
          echo "Tests passed!"
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build
        run: |
          echo "Building DevOps WebApp..."
          echo "Checking index.html..."
          test -f index.html
          echo "Build successful!"

      - name: Run tests
        run: |
          echo "Running tests..."
          test -f index.html
          echo "Tests passed!"
